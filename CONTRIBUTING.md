# Developer notes

## Docker setup

```bash
alias dr='docker-compose run --rm web'

# Build image
docker-compose build

# First-time setup
dr bundle install
```

Server:

```bash
# Start server
docker-compose up
```

Tests:

```bash
echo "lol, you wish"
```
